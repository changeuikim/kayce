---
title: '[CS] 프로그래밍 패러다임'
summary: '선언형 프로그래밍과 명령형 프로그래밍에 대해 알아봅니다'
date: '2024-10-28 09:53'
lastModified: '2024-10-28 09:53'
author: 'kayce'
tags: ['computer-science', 'paradigm', 'function', 'declarative']
order: [04, 01, 02]
---

<LanguageSelector />

## 프로그래밍 패러다임

> 프로그래밍 패러다임은 프로그래머에게 프로그래밍의 관점을 갖게 해주는 역할을 하는 개발 방법론입니다.

명령형 :
  - 절차지향 : 로직이 수행되어야 할 연속적인 계산 과정으로 이루어진 프로그래밍 패러다임입니다.
  - 객체지향 : 객체들의 집합으로 프로그램의 상호작용을 표현하는 프로그래밍 패러다임입니다.
선언형 :
  - 함수형 : 순수 함수들을 블록처럼 쌓아 로직을 구현하고 고차 함수를 통해 재사용성을 높인 프로그레밍 패러다임입니다.

## 함수형 프로그래밍

> 무엇을 풀어내는가에 집중하는 선언형 프로그래밍의 일종으로, 순수 함수와 고차 함수를 활용해 코드의 재사용성을 높입니다.

순수 함수 : 출력이 입력에만 의존하는 함수입니다. 매개변수 외의 전역변수 등에 영향을 받지 않아야합니다.

고차 함수 : 함수가 함수를 값처럼 매개변수로 받아 로직을 생성할 수 있는 것을 말합니다. 함수가 일급객체인 언어에서만 가능합니다.

일급 객체 : 
  - 변수나 메서드에 함수를 할당할 수 있습니다.
  - 함수 안에 함수를 매개변수로 할당할 수 있습니다.
  - 함수가 함수를 반환할 수 있습니다.

## 객체지향 프로그래밍

> 데이터를 객체로 취급하여 객체 내부에 선언된 메서드를 활용합니다.

설계에 많은 시간이 소요되며 처리 속도가 다른 프로그래밍 언어에 비해 상대적으로 느립니다. 

### 특징

추상화 : 복잡한 시스템으로부터 핵심적인 개념 또는 기능을 간추려내는 것을 말합니다.

캡슐화 : 객체의 속성과 메서드를 하나로 묶고 일부를 외부에 감추어 은닉하는 것을 말합니다.

상속성 : 상위 클래스의 특성을 하위 클래스가 이어받아 재사용하거나 추가 확장하는 것을 말합니다.

다형성 : 하나의 메서드나 클래스가 다양한 방법으로 동작하는 것을 말합니다.

#### 오버로딩과 오버라이딩

오버로딩 : 같은 이름을 가진 메서드를 여러개 두는 것을 말합니다. 컴파일 중 발생하는 정적 다형성입니다.

오버라이딩 : 상위 클래스로부터 상속받은 메서드를 하위 클래스가 재정의하는 것을 말합니다. 런타임 중 발생하는 동적 다형성입니다.

### 설계원칙

단일 책임 원칙 : 모든 클래스는 하나의 책임만 가져야합니다.

개방 폐쇄 원칙 : 코드를 쉽게 확장할 수 있어야하고, 수정에는 닫혀있어야 합니다.

리스코프 치환 원칙 : 프로그램의 객체는 프로그램의 정확성을 깨트리지 않으면서 하위 타입의 인스턴스로 바꿀 수 있어야 합니다.

인터페이스 분리 원칙 : 하나의 일반적인 인터페이스보다 구체적인 여러개의 인터페이스를 만들어야 합니다.

의존 역전 원칙 : 추상화된 클래스나 인터페이스를 두어 변화되기 쉬운 것의 영향을 받지 말아야 합니다.

